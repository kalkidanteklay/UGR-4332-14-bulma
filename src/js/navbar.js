const button = document.querySelector('#button');
const navbarMenu = document.querySelector('#mySidebar');
 
    button.addEventListener('click', () => {
        navbarMenu.classList.toggle('is-active');
    
        
})
